<Window x:Class="Nexus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Nexus - Personal Knowledge Base" Height="800" Width="1400"
        Background="#1a1625">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#7c3aed"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#8b5cf6"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#6d28d9"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2d2438"/>
            <Setter Property="Foreground" Value="#e9d5ff"/>
            <Setter Property="BorderBrush" Value="#7c3aed"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#a78bfa"/>
        </Style>
        
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="#2d2438"/>
            <Setter Property="Foreground" Value="#e9d5ff"/>
            <Setter Property="BorderBrush" Value="#7c3aed"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e9d5ff"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Blessing Text -->
        <Border Grid.Row="0" Background="#0f0a1a" Padding="8">
            <TextBlock Text="In the Name of God the Most Glorious the Most High" 
                       Foreground="#a78bfa" 
                       FontSize="13" 
                       FontStyle="Italic"
                       HorizontalAlignment="Center"
                       FontWeight="Medium"/>
        </Border>

        <Border Grid.Row="1" Background="#2d2438" Padding="12" BorderBrush="#7c3aed" BorderThickness="0,0,0,2">
            <StackPanel Orientation="Horizontal">
                <Button Content="➕ New Note" Click="NewNote_Click"/>
                <Button Content="🗑️ Delete Note" Click="DeleteNote_Click" Background="#dc2626">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="Background" Value="#dc2626"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#ef4444"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="🔍 Search" Click="Search_Click"/>
                <Button Content="🔄 Sync" Click="Sync_Click" Background="#10b981">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="Background" Value="#10b981"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#34d399"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Rectangle Width="2" Fill="#7c3aed" Margin="8,4"/>
                <CheckBox x:Name="GraphToggle" Content="📊 Graph View" 
                          Checked="ToggleGraph_Checked" Unchecked="ToggleGraph_Unchecked" 
                          VerticalAlignment="Center" Margin="8,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*" x:Name="GraphColumn"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#2d2438" BorderBrush="#7c3aed" BorderThickness="0,0,2,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="📝 Notes" Foreground="#e9d5ff" FontSize="18" FontWeight="Bold" 
                               Margin="16,16,16,12"/>
                    
                    <ListBox x:Name="NotesList" Grid.Row="1" SelectionChanged="NotesList_SelectionChanged" 
                             Margin="8" BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#1a1625" Padding="12" Margin="4" CornerRadius="6"
                                        BorderBrush="#7c3aed" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" 
                                                   Foreground="#e9d5ff" FontSize="15" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding UpdatedAt, StringFormat='Updated: {0:g}'}" 
                                                   FontSize="11" Foreground="#a78bfa" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="#1a1625" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="✏️ Editor" Foreground="#e9d5ff" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>
                    
                    <TextBox x:Name="TitleBox" Grid.Row="1" FontSize="22" FontWeight="SemiBold" 
                             Margin="0,0,0,16" Height="50"/>
                    
                    <Border Grid.Row="2" BorderBrush="#7c3aed" BorderThickness="2" CornerRadius="6">
                        <TextBox x:Name="ContentBox" AcceptsReturn="True" TextWrapping="Wrap" 
                                 VerticalScrollBarVisibility="Auto" BorderThickness="0"
                                 Background="#2d2438" Foreground="#e9d5ff"/>
                    </Border>
                    
                    <Button Grid.Row="3" Content="💾 Save Note" Click="Save_Click" 
                            Margin="0,16,0,0" Height="44" FontSize="16"/>
                </Grid>
            </Border>

            <Border Grid.Column="2" Background="#0f0a1a" BorderBrush="#7c3aed" BorderThickness="2,0,0,0">
                <wv2:WebView2 x:Name="GraphWebView" Visibility="Collapsed"/>
            </Border>
        </Grid>
    </Grid>
</Window>
